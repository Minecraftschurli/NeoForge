--- a/net/minecraft/server/jsonrpc/internalapi/MinecraftApi.java
+++ b/net/minecraft/server/jsonrpc/internalapi/MinecraftApi.java
@@ -16,7 +_,11 @@
     private final MinecraftServerSettingsService minecraftServerSettingsService;
     private final MinecraftServerStateService minecraftServerStateService;
     private final MinecraftExecutorService executorService;
+    private final @org.jspecify.annotations.Nullable DedicatedServer server;
+    private final @org.jspecify.annotations.Nullable JsonRpcLogger logger;
 
+    /** @deprecated Neo: Use the constructor with the server and logger instead **/
+    @Deprecated
     public MinecraftApi(
         NotificationManager notificationManager,
         MinecraftAllowListService allowListService,
@@ -28,6 +_,22 @@
         MinecraftServerStateService minecraftServerStateService,
         MinecraftExecutorService executorService
     ) {
+        this(notificationManager, allowListService, banListService, minecraftPlayerListService, gameRuleService, minecraftOperatorListService, minecraftServerSettingsService, minecraftServerStateService, executorService, null, null);
+    }
+
+    public MinecraftApi(
+            NotificationManager notificationManager,
+            MinecraftAllowListService allowListService,
+            MinecraftBanListService banListService,
+            MinecraftPlayerListService minecraftPlayerListService,
+            MinecraftGameRuleService gameRuleService,
+            MinecraftOperatorListService minecraftOperatorListService,
+            MinecraftServerSettingsService minecraftServerSettingsService,
+            MinecraftServerStateService minecraftServerStateService,
+            MinecraftExecutorService executorService,
+            @org.jspecify.annotations.Nullable DedicatedServer server,
+            @org.jspecify.annotations.Nullable JsonRpcLogger logger
+    ) {
         this.notificationManager = notificationManager;
         this.allowListService = allowListService;
         this.banListService = banListService;
@@ -37,6 +_,8 @@
         this.minecraftServerSettingsService = minecraftServerSettingsService;
         this.minecraftServerStateService = minecraftServerStateService;
         this.executorService = executorService;
+        this.server = server;
+        this.logger = logger;
     }
 
     public <V> CompletableFuture<V> submit(Supplier<V> supplier) {
@@ -79,6 +_,20 @@
         return this.notificationManager;
     }
 
+    public DedicatedServer getServer() {
+        if (this.server == null) {
+            throw new net.minecraft.server.jsonrpc.methods.MethodNotFoundJsonRpcException("This method requires the server, but it is not available");
+        }
+        return this.server;
+    }
+
+    public JsonRpcLogger getLogger() {
+        if (this.logger == null) {
+            throw new net.minecraft.server.jsonrpc.methods.MethodNotFoundJsonRpcException("This method requires the json-rpc logger, but it is not available");
+        }
+        return this.logger;
+    }
+
     public static MinecraftApi of(DedicatedServer server) {
         JsonRpcLogger jsonrpcLogger = new JsonRpcLogger();
         MinecraftAllowListServiceImpl allowListService = new MinecraftAllowListServiceImpl(server, jsonrpcLogger);
@@ -98,7 +_,9 @@
             operatorListService,
             serverSettingsService,
             serverStateService,
-            executorService
+            executorService,
+            server,
+            jsonrpcLogger
         );
     }
 }
